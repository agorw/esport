{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    
    <div class="inscription"> </div>
    <h1> Inscription </h1>

    {{ form_start(registrationForm) }}  
        {{ form_row(registrationForm.email, { 'label': 'E-mail' }) }}         
        {{ form_row(registrationForm.pseudo, { 'label': 'Pseudo' }) }}       
        {{ form_row(registrationForm.Password, { 'label': 'Mot de Passe' }) }}
        {{ form_row(registrationForm.telephone, { 'label': 'Numéro de téléphone' }) }}    

        <button class="btn btn-primary mb-3">Valider</button>
        
    {{ form_end(registrationForm) }}
{% endblock %}

